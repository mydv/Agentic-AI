apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc.logs.name }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pvc.logs.storage }}